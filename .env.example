# Environment Variables
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit .env to git (it's in .gitignore)

# Node Environment
NODE_ENV=development
PORT=3001

# Database (MySQL required for all environments)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-mysql-password
DB_NAME=moms_recipes

# Frontend URL (for CORS - required in production)
# FRONTEND_URL=https://moms-recipes.tachyonfuture.com

# Authentication & Security
# IMPORTANT: In production, JWT_SECRET must be at least 32 characters
JWT_SECRET=your-secret-key-change-in-production-min-32-chars

# CSRF Protection
# IMPORTANT: In production, CSRF_SECRET is required
CSRF_SECRET=your-csrf-secret-change-in-production

# AI Provider API Keys (at least one required for AI features)
# AI features include: PDF recipe parsing, URL recipe extraction, calorie estimation
# You can configure the provider via Admin Panel > Settings > AI Settings
# Or set environment variables below (database settings take precedence)

# Anthropic API (Claude) - Recommended
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# OpenAI API (GPT-4) - Alternative
# OPENAI_API_KEY=your-openai-api-key-here

# Google AI API (Gemini) - Alternative
# GOOGLE_API_KEY=your-google-api-key-here

# Admin Users
# First admin user (required for initial setup)
ADMIN1_USERNAME=your-admin-username
ADMIN1_PASSWORD=your-secure-password
ADMIN1_EMAIL=your-email@example.com

# Second admin user (optional)
ADMIN2_USERNAME=second-admin-username
ADMIN2_PASSWORD=second-secure-password
ADMIN2_EMAIL=second-email@example.com
